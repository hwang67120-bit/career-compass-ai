# Claude Code 작업 기준

작업을 시작하기 전에 프로젝트 최상위의 `AGENTS.md`를 읽고 따른다.

- 요청받은 디렉터리와 파일만 수정한다.
- Java 작업 위치는 `backend-java`이다.
- Python 작업 위치는 `ai-python`이다.
- 서버 간 요청·응답 변경은 `contracts`를 먼저 수정한다.
- `contracts`, `deploy`, 최상위 설정 파일은 공용 영역이므로 담당 범위를 확인한 후 수정한다.
- 관련 정책이나 API 명세가 없으면 임의로 구현하지 말고 `확인 필요`로 보고한다.
- 다른 AI가 작업 중인 파일을 덮어쓰지 않는다.
