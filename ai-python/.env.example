GEMINI_API_KEY=

# 확인 필요: 사용할 Ollama 모델 이름 (채용공고 직무명·기술 추출용, 예: qwen2.5:latest)
OLLAMA_MODEL=

# 채용공고 "담당 업무"만 추출하는 별도 호출 전용 모델 — OLLAMA_MODEL과 다른 모델을
# 쓴다. 2026-08-03 확인: qwen2.5는 이 호출에서 evidence를 계속 못 채웠고(4회+ 재현),
# exaone3.5는 6/6 성공했다. docs/current-work.md 참고, 확인 필요(표본 작음)
OLLAMA_JOB_POSTING_RESPONSIBILITY_MODEL=

# 선택: 기본값 http://127.0.0.1:11434. 다른 주소를 쓸 때만 지정한다.
# OLLAMA_BASE_URL=

# 확인 필요: 사용할 Gemini 모델 이름 (예: gemini-2.5-flash)
GEMINI_MODEL=

# 확인 필요: 사용할 Ollama 임베딩 모델 이름 (예: nomic-embed-text:latest) — 실험 결과 한국어 도메인 구분력이 낮았음, docs/architecture/embedding-similarity.md 참고
OLLAMA_EMBEDDING_MODEL=

# 확인 필요: 사용할 Gemini 임베딩 모델 이름 (예: models/gemini-embedding-001)
GEMINI_EMBEDDING_MODEL=

# 확인 필요: Java와 공유하는 내부 서비스 인증 토큰 (충분히 길고 무작위한 값)
INTERNAL_SERVICE_TOKEN=

# 확인 필요: 채용공고 계약이 제안 상태라 임시값. 텍스트 최대 길이(문자 수)
JOB_POSTING_EXTRACTION_MAX_TEXT_LENGTH=
