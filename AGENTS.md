# AI 협업 규칙

## 기준 문서

- 코드 작업에서는 프로젝트 안의 문서를 최종 기준으로 사용한다.
- 기준 우선순위는 `프로젝트 문서 > 노션의 현재 사용 문서 > AI 대화 기록`이다.
- 확정되지 않은 내용은 `제안` 또는 `확인 필요`로 표시한다.
- AI의 추론을 확정된 정책처럼 구현하지 않는다.

## 프로젝트 영역

- `backend-java`: Java 서버, 인증·인가, 비즈니스 정책, 데이터 저장, 최종 판정
- `ai-python`: 문서 처리, 정보 추출, 임베딩, 유사도 계산, LLM 실행
- `contracts`: Java와 Python 사이의 요청·응답 계약
- `deploy`: Docker Compose와 배포 설정
- `docs`: API 명세, ERD, 정책과 설계 문서

Java 작업은 `backend-java`, Python 작업은 `ai-python`에서만 수행한다.
서버 간 통신 형식을 변경할 때는 `contracts`를 먼저 수정하고 양쪽 구현을 맞춘다.

## 작업 분담

- 하나의 파일이나 도메인은 동시에 한 AI만 수정한다.
- AI 이름에 역할을 영구적으로 고정하지 않고 작업마다 담당 범위를 지정한다.
- 담당하지 않은 영역의 문제는 직접 수정하지 말고 보고한다.
- 사용자나 다른 AI의 기존 변경사항을 임의로 삭제하거나 덮어쓰지 않는다.
- 제품 목표, 기술 스택, API 호환성, 데이터 저장 또는 보안 정책 변경은 사용자 승인을 받는다.

## 구현 원칙

- 하나의 API 또는 하나의 기능을 한 작업 단위로 사용한다.
- 관련 API 명세와 비즈니스 정책을 먼저 확인한다.
- 공식 문서와 일반적으로 검증된 구현 방식을 사용한다.
- 명세에 없는 기능과 검증되지 않은 추론 로직을 임의로 추가하지 않는다.
- API 키, 비밀번호, 토큰, 암호화 키와 운영 서버 정보는 하드코딩하지 않는다.
- 비밀정보는 환경변수로 주입하고 실제 값이 든 설정 파일은 Git에 올리지 않는다.

## 검증 순서

1. 컴파일 또는 문법 오류를 확인한다.
2. 변경 기능의 단위 테스트를 실행한다.
3. 주요 런타임 오류와 예외 처리를 확인한다.
4. 필요한 기능만 동시성과 데이터 일관성을 확인한다.
5. 비즈니스 정책과 API 계약 준수 여부를 확인한다.
6. 코드·테스트·로그의 보안정보 하드코딩을 확인한다.

실행하지 않은 테스트를 완료했다고 보고하지 않는다. 테스트 실패를 숨기거나 테스트 조건을 약화하지 않는다.

## 환경

- 프로젝트 파일과 문서는 Windows 공유 폴더에 보관한다.
- Java와 Python 코딩 작업은 Linux 개발 환경에서 수행한다.
- IntelliJ에서는 `backend-java`만 연다.
- Python 개발 도구에서는 `ai-python`만 연다.
- 제품 최상위 폴더를 하나의 IntelliJ 프로젝트로 열지 않는다.

## 완료 보고

작업을 마치면 수정한 파일, 구현한 기능, 실행한 테스트, 실행하지 못한 테스트, 남은 위험을 짧게 보고한다.
