# AI 협업 규칙

## 목적

이 문서는 AI 취업 방향 분석 서비스에서 AI가 코드와 문서를 변경할 때 반드시 지켜야 할 실행 규칙이다.
상세 정책을 임의로 해석하거나 대화 내용을 확정 정책으로 바꾸지 않는다.

## 기준 문서와 우선순위

- 코드 작업에서는 프로젝트 안의 문서를 최종 기준으로 사용한다.
- 기준 우선순위는 `프로젝트 문서 > Notion의 현재 사용 문서 > AI 대화 기록`이다.
- Notion에서 상태가 `현재 사용`인 문서만 구현 기준으로 사용한다.
- `검토 필요`, 예시 코드와 일정표는 구현 후보 또는 참고 자료이며 확정 정책보다 우선하지 않는다.
- 문서끼리 API 필드, 상태값, 계산식 또는 보안 규칙이 충돌하면 구현을 중단하고 `확인 필요`로 보고한다.
- 확정되지 않은 내용은 `제안` 또는 `확인 필요`로 표시한다.
- AI의 추론을 확정된 정책처럼 구현하지 않는다.

### 현재 정책 출처

- 비즈니스 정책: https://app.notion.com/p/3a417af94e528139a931e345c5e1eaed
- 코드 작성 규칙: https://app.notion.com/p/3a417af94e52812a9fbddc6a2420e0e9
- Spring Security JWT: https://docs.spring.io/spring-security/reference/servlet/oauth2/resource-server/jwt.html
- Spring Security 비밀번호 저장: https://docs.spring.io/spring-security/reference/features/authentication/password-storage.html

## 문서 용어 표시 규칙

- 프로젝트 공통 영어·코드 용어의 한글 뜻은 [공통 용어집](docs/glossary.md)을 기준으로 사용한다.
- README, `docs`, `contracts`와 PR 설명에서 영어 용어가 처음 등장할 때 `영어(한글 뜻)` 형식으로 작성한다.
- 코드 식별자, JSON 필드, 환경변수, enum과 로그 값은 영어 원문을 유지하고 표나 설명에서 한글 뜻을 함께 제공한다.
- 한글 번역 없이 영어 용어만 나열하거나, 같은 영어 용어를 문서마다 다른 한글 뜻으로 번역하지 않는다.
- 새 공통 용어를 도입하면 코드·계약 변경 전에 용어집의 뜻과 책임 범위를 먼저 추가한다.
## 프로젝트 영역

- `backend-java`: Java 서버, 사용자 API, 인증·인가, 비즈니스 정책, 데이터 저장, Python 작업 제어와 최종 판정
- `ai-python`: 문서 처리, 정보 추출, 분류, 임베딩, 의미 유사도와 LLM 실행
- `contracts`: Java와 Python 사이의 요청·응답 JSON 계약과 공통 예제
- `deploy`: Docker Compose와 배포 설정
- `docs`: API 명세, ERD, 정책, 설계와 결정 기록

Java 작업은 `backend-java`, Python 작업은 `ai-python`에서만 수행한다.
서버 간 통신 형식을 변경할 때는 `contracts`를 먼저 수정하고 양쪽 구현과 계약 테스트를 맞춘다.

## 작업 분담

- 작업을 시작하기 전에 담당 디렉터리, 도메인과 파일을 선언한다.
- 하나의 파일이나 도메인은 동시에 한 AI만 수정한다.
- 하나의 API 또는 하나의 기능을 한 작업 단위로 사용한다.
- 담당하지 않은 영역의 문제는 직접 수정하지 말고 영향과 필요한 변경을 보고한다.
- 사용자나 다른 AI의 기존 변경사항을 임의로 삭제하거나 덮어쓰지 않는다.
- 제품 목표, 기술 스택, API 호환성, 데이터 저장 또는 보안 정책 변경은 사용자 승인을 받는다.

## 구현 시작 조건

다음 항목을 확인하기 전에는 코드를 작성하지 않는다.

1. 대상 API 또는 기능의 요청, 응답과 상태 코드
2. 관련 비즈니스 정책과 데이터 상태
3. 데이터 소유자와 접근 권한
4. 저장할 값, 임시 보관할 값과 저장하지 않을 값
5. Java–Python 통신 여부와 `contracts` 존재 여부
6. 정상, 오류, 부분 완료와 재시도 조건
7. 변경 단위에서 실행할 테스트

명세가 없거나 서로 충돌하면 값을 추측해서 DTO, enum, 테이블 또는 분기문을 만들지 않는다.

# 비즈니스 정책

## 사실, 추정과 미확인 구분

- 사용자 문서, 채용공고와 회사 공식 자료에서 직접 확인한 내용만 사실로 저장한다.
- 주요 결과는 `확인됨`, `추정됨`, `확인할 수 없음` 상태를 구분한다.
- 추정 결과에는 사용한 근거를 연결한다.
- 근거가 없거나 자료가 비어 있으면 내용을 생성하지 않는다.
- 사용자 사실, 사용자 희망, 회사 사실, 채용 사실, 시장 통계와 AI 추정을 서로 다른 데이터로 관리한다.
- AI 추출 결과는 사용자가 확인하기 전까지 확정 프로필로 사용하지 않는다.
- 원문 값과 정규화 값을 함께 보존하고 AI 결과로 원문 사실을 덮어쓰지 않는다.

## 사용자 자료

- 이력서와 포트폴리오에 없는 기술, 경력, 역할과 성과를 보유한 것으로 처리하지 않는다.
- 프로젝트 전체 기술과 사용자가 직접 담당한 기술을 구분한다.
- 사용 기간과 숙련도를 확인할 수 없으면 임의로 정하지 않는다.
- 사용자 희망 조건은 이력서에서 확인한 사실과 분리한다.
- 추출 결과는 사용자가 확인하고 수정할 수 있어야 한다.
- 자기소개 전체를 비교 데이터로 저장하지 않고 기술, 업무, 경력과 프로젝트를 입증하는 최소 근거만 사용한다.

## 회사와 채용공고

- 회사 전체 사업과 특정 채용공고의 직무·담당 업무를 분리한다.
- 필수 조건과 우대 조건을 분리한다.
- 공고 홍보 문구를 실제 근무 조건으로 확정하지 않는다.
- 공고에 없는 문화, 연봉, 복지와 합격 기준을 생성하지 않는다.
- 회사명, 원문 직무명, 게시일, 마감일, 출처와 마지막 확인 시각을 기록한다.
- 수정된 공고는 이전 버전을 덮어쓰지 않고 변경 이력을 남긴다.
- 동일 공고를 여러 출처에서 수집해 시장 수요를 부풀리지 않는다.
- 마감, 삭제, 상시 채용과 상태 확인 필요를 구분한다.

## 조건 판정

Java가 다음과 같이 명확한 조건을 규칙으로 판정한다.

- 신입·경력과 경력 기간
- 학력
- 필수·우대 자격증
- 필수·우대 기술
- 근무 지역
- 고용 형태
- 지원 마감일

사용자 데이터 상태는 `보유`, `미보유`, `미입력`으로 구분한다.
비교 결과는 다음 규칙을 적용한다.

- `일치`: 회사 요구와 사용자 근거가 일치함, 1점
- `불일치`: 회사 요구가 있고 사용자가 미보유함, 0점
- `확인 필요`: 사용자 또는 공고 정보가 부족함, 점수 제외
- `해당 없음`: 회사가 해당 항목을 명시하지 않음, 점수 제외

MVP에서는 검증된 근거 없이 0.5점 같은 중간 점수를 추가하지 않는다.

`조건 일치율 = 일치한 비교 항목 수 / 비교 가능한 항목 수`

- 비교 가능한 항목은 1점 또는 0점으로 판정된 항목만 포함한다.
- `확인 필요`와 `해당 없음`은 분모와 분자에서 모두 제외한다.
- 결과에는 비교 가능, 일치, 불일치, 확인 필요와 제외 항목 및 이유를 표시한다.
- 필수조건 일치율과 우대조건 일치율을 분리한다.
- 필수조건이 하나라도 명확히 불일치하면 `필수조건 불충족`으로 표시한다.
- 우대조건 불일치는 지원 불가가 아니라 `선택적 보완`으로 표시한다.
- 필수조건이 미입력이면 충족 또는 불충족을 단정하지 않는다.
- 회사가 명시하지 않은 항목을 0점으로 만들지 않는다.

## 의미 유사도와 AI 설명

- 지역·경력·학력·자격증 같은 명확한 조건은 Java가 판정한다.
- 업무, 기술과 프로젝트 경험의 의미 유사도는 Python이 계산한다.
- 비교 순서는 필수조건, 주요 업무, 요구 기술, 프로젝트 근거, 보조 직무명 순서다.
- 기술 이름이 같다는 이유만으로 실제 업무가 같다고 판단하지 않는다.
- 직무명과 직무 분류는 검색·통계·표시를 위한 보조 정보이며 지원 가능 판정의 필수값이 아니다.
- 조건 판정과 의미 유사도를 하나의 불투명한 총점으로만 합치지 않는다.
- 유사도는 합격 확률, 채용 담당자의 평가, 지원자 순위 또는 실제 수행 능력 보장이 아니다.
- LLM은 Java가 확정한 수치, 조건 판정과 시장 통계를 변경할 수 없다.
- 분석 데이터에 없는 회사와 출처 없는 전략을 생성하지 않는다.

## 근거와 출처

- 모든 주요 추출값, 조건 판정과 유사도 결과는 근거 식별자와 연결한다.
- 근거 문장은 판정을 확인할 수 있는 최소 범위만 저장한다.
- 직접 적힌 내용과 AI가 해석한 내용을 구분한다.
- 근거가 없으면 0점을 만들지 않고 근거 없음 또는 확인 필요로 처리한다.
- 외부 자료에는 출처, 수집 시각, 마지막 확인 시각과 문서 버전을 기록한다.

## 개인정보와 보관

- 사용자 자료는 사용자별로 격리하고 자신의 자료만 조회·수정·삭제할 수 있게 한다.
- 이름, 전화번호, 이메일, 생년월일, 사진과 상세 주소는 점수와 의미 유사도에 사용하지 않는다.
- 주민등록번호, 상세 주소, 계좌와 분석에 필요 없는 민감정보는 구조화 프로필, 로그, 임베딩, 캐시와 학습 데이터에 저장하지 않는다.
- 거주 지역은 사용자 동의가 있을 때 시·구 단위까지만 사용한다.
- Python과 외부 AI에는 개인정보가 제거된 최소 필드와 최소 근거만 전달한다.
- 원본 문서는 안전 검사와 추출에 필요한 임시 데이터로 취급한다.
- 별도 보관 동의와 보관 기간이 확정되지 않았으면 원문을 영구 저장하는 구현을 추가하지 않는다.
- 사용자 문서와 피드백을 동의 없이 공통 모델 학습에 사용하지 않는다.
- 일반 로그에 원문 전체, 비밀번호, JWT, 인증키와 개인정보를 기록하지 않는다.

## 시장 통계와 추천

- 모든 시장 수치에는 분석 기간, 공고 표본 수, 회사 수, 출처, 범위와 갱신 시각을 표시한다.
- 중복 제거 기준을 적용하지 않은 공고 수를 시장 수요로 사용하지 않는다.
- 지원자 수와 채용 인원이 실제 출처에 있을 때만 경쟁률을 계산한다.
- 자료가 없으면 `경쟁 확인 불가`와 `진입 난이도 추정`을 구분한다.
- 필수조건을 명백히 충족하지 못한 회사를 유사도만으로 높은 우선순위에 올리지 않는다.
- 추천 결과에는 포함 또는 제외 이유를 표시한다.
- 광고나 제휴를 이유로 특정 회사와 기술을 우선하지 않는다.

## 오류, 부분 결과와 이용량

- 일부 단계만 성공하면 성공 결과와 실패 결과를 분리해 `부분 완료`로 표시한다.
- 빈 값을 추측으로 채우지 않는다.
- 재시도해도 같은 결과가 보장되는 멱등 작업만 자동 재시도한다.
- 입력 검증 단계에서 AI가 실행되지 않은 요청과 서버·모델 장애로 결과를 제공하지 못한 요청은 무료 이용량에서 차감하지 않는다.
- 서버 보호 제한, 무료 이용량과 모델 토큰 제한을 하나의 값으로 합치지 않는다.
- 제한값은 실제 측정 결과 없이 코드에 고정하지 않는다.

# Java 코드 작성 규칙

## 기술과 구조

- Java 실행 기준은 Java 21이다.
- Spring Boot, Spring Security, Spring Data JPA, PostgreSQL과 Flyway의 공식 사용법을 우선한다.
- Controller는 HTTP 입력·출력, Service는 유스케이스와 트랜잭션, Entity는 저장 상태, Repository는 저장소 접근을 담당한다.
- 비즈니스 판정은 Controller, Entity getter 또는 예외 처리기에 분산하지 않는다.
- 외부 API와 Python 호출 객체를 도메인 객체로 직접 사용하지 않는다.
- 설정값은 `ConfigurationProperties` 같은 명시적인 설정 객체로 관리한다.

## 이름 규칙

- Java 클래스는 UpperCamelCase, 메서드와 변수는 lowerCamelCase를 사용한다.
- enum 값과 공통 상수는 UPPER_SNAKE_CASE를 사용한다.
- 이름에는 동작과 대상을 함께 표현한다.
- 같은 개념은 프로젝트 전체에서 같은 영어 단어를 사용한다.
- `tmp`, `data`, `info`, `value`, `mgr`, `calc`, `proc`, `impl`, 한 글자 변수와 대상 없는 `process`, `handle`, `run`을 직접 만든 이름에 사용하지 않는다.
- `auth`처럼 여러 의미가 있는 축약어는 직접 만든 도메인 이름에 사용하지 않는다. 표준 프로토콜과 프레임워크 공식 이름은 예외다.

프로젝트 공통 용어는 다음 이름을 사용한다.

- `userProfile`: 사용자가 확인한 기술, 경력, 프로젝트, 학력과 자격증
- `jobPosting`: 하나의 채용공고
- `companyProfile`: 회사 사업, 제품과 고객 정보
- `jobRequirement`: 채용공고의 필수·우대·근무 조건
- `conditionResult`: 명확한 조건 판정 결과
- `similarityResult`: 기술·업무·프로젝트 의미 유사도 결과
- `marketMetric`: 공고 표본으로 계산한 시장 지표
- `evidence`: 사용자 문서, 공고 또는 회사 자료의 근거
- `source`: 자료 제공 위치와 확인 시각
- `analysisResult`: 조건, 유사도, 시장 지표와 설명을 합친 결과

동작 이름은 다음 의미를 유지한다.

- `find`: 조건에 맞는 내부 데이터 또는 저장소 조회
- `get`: 이미 가진 객체나 값의 단순 반환
- `fetch`: 외부 API 또는 원격 자원 요청
- `list`: 여러 항목 반환
- `retrieve`: 식별자로 한 항목 조회
- `search`: 검색어와 관련도 기반 탐색
- `read`: 파일 또는 문서 내용 읽기
- `create`: 새 자원 생성
- `update`: 기존 자원의 여러 값 변경
- `delete`: 자원 삭제
- `save`: 저장소의 신규·기존 자원 저장
- `extract`: 문서에서 구조화 항목 추출
- `classify`: 직무, 기술 또는 회사 유형 분류
- `normalize`: 같은 의미의 표현을 대표 형식으로 정리
- `compare`: 두 대상을 항목별 비교
- `calculate`: 규칙과 수식으로 계산
- `generate`: 검증된 데이터로 설명 또는 후보 생성
- `validate`: 잘못된 입력이면 중단하는 검증
- `check`: 상태를 확인하고 결과를 반환하는 검사
- `verify`: 외부 결과나 근거를 이용한 사실 확인
- boolean 반환은 `is`, `has`, `can`으로 시작한다.

## 주석 규칙

- Java 주석은 Service와 Config 코드에만 작성한다.
- Entity, DTO, Controller와 Global Exception에는 규칙상 주석을 작성하지 않는다.
- 한 줄 주석 대신 블록 주석을 사용한다.
- Service 기능 메서드에는 기능과 반환값만 작성한다.
- 반환값이 없으면 반환값이 없다고 표시한다.
- Config에는 기능만 작성하고 반환값 설명은 작성하지 않는다.
- 구현 과정을 코드와 같은 말로 반복하지 않는다.

## 하드코딩과 설정

- 운영 동작을 결정하는 숫자, 경로, 주소, 모델, 임계값, 제한시간, 재시도 횟수, 최대 크기와 메시지를 코드에 직접 작성하지 않는다.
- 환경별 값은 설정 파일과 환경변수로 관리한다.
- 프로젝트 공통 고정값은 의미 있는 상수, 상태와 종류는 enum, 여러 조건은 정책·설정 객체로 관리한다.
- 비밀키, 토큰과 실제 비밀번호는 소스, 일반 설정 파일, 테스트와 로그에 저장하지 않는다.
- 테스트 값은 테스트 데이터와 테스트 설정에서 관리한다.

## 공식 근거와 복잡도

- Java, Spring과 라이브러리는 공식 문서를 먼저 확인한다.
- 공식 문서에 없는 동작은 공식 저장소의 구현, 테스트와 변경 이력을 추가로 확인한다.
- 출처를 확인하지 못한 블로그, AI 답변과 기억에 의존해 운영 코드를 작성하지 않는다.
- AI가 생성한 로직은 테스트 전까지 구현 후보로 취급한다.
- 측정 결과 없는 최적화, 비동기 처리, 캐시, 재시도와 동시성 제어를 추가하지 않는다.
- 표준 구현으로 요구사항을 만족할 수 있으면 검증되지 않은 자체 프레임워크를 만들지 않는다.

# 기본 인증·JWT 보안

이 절은 기본 보안만 정의한다. 토큰 재발급, 다중 기기, 키 회전 주기와 세부 권한 모델은 별도 정책이 확정된 뒤 구현한다.

- 비밀번호는 Spring Security `PasswordEncoder`의 단방향 적응형 해시로 저장한다.
- 비밀번호 원문, 복호화 가능한 비밀번호와 `NoOpPasswordEncoder`를 운영 코드에 사용하지 않는다.
- JWT 서명키, 공개키 위치, 발급자, 대상 서버와 토큰 수명은 환경변수 또는 보안 설정으로 주입한다.
- JWT 서명 알고리즘은 서버 설정에서 허용한 알고리즘만 사용하고 토큰 입력만 보고 신뢰 대상을 바꾸지 않는다.
- 액세스 토큰에는 최소한 `sub`, `jti`, `iat`, `exp`를 포함한다.
- 요청 수신 시 서명, `iss`, `exp`, `nbf`를 검증한다. 대상 서버가 확정되면 `aud`도 검증한다.
- 사용자 식별자는 검증이 끝난 `sub`에서 읽고 UUID 형식과 사용자 상태를 확인한다.
- 인증되지 않은 요청은 401, 인증됐지만 권한이 없는 요청은 403으로 구분한다.
- 토큰 원문을 데이터베이스, 일반 로그, 오류 응답과 URL에 저장하거나 노출하지 않는다.
- 로그아웃 블랙리스트가 필요한 경우 JWT 원문이 아니라 `jti`, 사용자 식별자, 폐기 시각과 만료 시각만 저장한다.
- 블랙리스트의 만료된 항목은 정리하며, 아직 만료되지 않은 폐기 `jti`는 401로 거부한다.
- 개발·테스트 사용자 우회는 `dev`와 `test` 프로필에서만 허용하고 운영 프로필에서는 Bean이 생성되지 않는 테스트를 둔다.
- CSRF 비활성화는 인증 전달 방식과 클라이언트 구조를 확인한 뒤 명시적으로 결정한다. 운영 보안을 편의상 전체 비활성화하지 않는다.
- 인증 실패 응답에 내부 예외, 키 정보, 토큰 내용과 사용자 존재 여부를 노출하지 않는다.

# 테스트와 검증

## 테스트 이름

- 테스트 이름은 검사 대상, 조건과 기대 행동 순서로 읽을 수 있게 작성한다.
- 테스트 이름만 보고 실패 원인을 추측할 수 있어야 한다.
- 정상 입력, 경계값, 잘못된 입력, 권한 없음, 상태 충돌과 외부 서버 실패를 구분한다.

## 필수 검증 순서

1. 컴파일 또는 문법 오류를 확인한다.
2. 변경 기능의 단위 테스트를 실행한다.
3. API 요청·응답과 상태 코드 테스트를 실행한다.
4. 저장소가 있으면 실제 PostgreSQL이 필요한 통합 테스트를 실행한다.
5. 주요 런타임 오류와 예외 처리를 확인한다.
6. 필요한 기능만 동시성과 데이터 일관성을 확인한다.
7. 비즈니스 정책과 API 계약 준수 여부를 확인한다.
8. 코드·테스트·설정·로그의 보안정보 하드코딩을 확인한다.
9. Java–Python 변경이면 양쪽에서 같은 예제 JSON으로 계약 테스트를 실행한다.

테스트 실패를 숨기거나 테스트 조건을 약화하지 않는다.
실행하지 않은 테스트를 완료했다고 보고하지 않는다.
테스트를 실행하지 못하면 원인, 미검증 범위와 실행할 명령을 보고한다.

## 완료 판정 기준

하나의 기능은 아래 단계를 통과해야 완료로 본다. 현재는 1~3단계까지만 적용하고, 4단계는 배포를 시작하는 시점에 적용한다.

1. AI 자체 테스트: Java와 Python이 각자 단위 테스트로 런타임 오류를 확인한다. 이 단계만 통과한 상태는 완료로 보지 않는다.
2. 연결 테스트: Java와 Python 서버를 함께 실행한 상태에서 실제 요청을 보내 계약대로 데이터가 오가는지 확인한다(예: 포스트맨). 한쪽 단위 테스트 통과만으로 연결이 검증됐다고 보지 않는다.
3. 브라우저 테스트: 실제 브라우저에서 서비스가 정상적으로 동작하는지 확인한다.
4. 배포 테스트: AWS 또는 임시로 구성한 서버에 올린 뒤 다른 사용자에게도 동일하게 동작하는지 확인한다. (추후 적용)

# 환경

- 프로젝트 파일과 문서는 Windows 공유 폴더에 보관한다.
- Java와 Python 코딩, 빌드와 Docker 실행은 Linux 개발 환경에서 수행한다.
- 사용자가 코드 작성만 요청하고 Linux 실행을 나중에 수행한다고 명시하면 파일 작성 후 테스트 미실행을 보고한다.
- IntelliJ에서는 `backend-java`만 연다.
- Python 개발 도구에서는 `ai-python`만 연다.
- 제품 최상위 폴더를 하나의 IntelliJ 프로젝트로 열지 않는다.
- 생성물인 `.idea`, `.gradle`, `build`, `.venv`와 `__pycache__`는 Git에 포함하지 않는다.

# 완료 보고

작업을 마치면 다음을 짧게 보고한다.

- 수정한 파일
- 구현한 기능 또는 반영한 정책
- 실행한 컴파일과 테스트
- 실행하지 못한 테스트와 이유
- API·계약·보안·데이터 관련 남은 위험
- 확정되지 않아 구현하지 않은 항목